name: Build Test

on:
  workflow_call:

jobs:
  build-test:
    name: Build & Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Build
        run: dotnet build --configuration Release -p:WarningLevel=0

      - name: Test
        run: |
          dotnet test \
            --configuration Release \
            --no-build \
            --hangdump \
            --hangdump-timeout 1m \
            --report-xunit-trx \
            --report-xunit-trx-filename fake-test-results.trx \
            --coverage \
            --coverage-output-format cobertura \
            --coverage-output fake-coverage.cobertura.xml

      - name: Test Kubernetes E2E
        run: |
          dotnet test \
            --project tests/KubeUI.Kubernetes.Tests/KubeUI.Kubernetes.Tests.csproj \
            --configuration Release \
            --no-build \
            --filter-trait "Category=Kind" \
            --max-threads 1 \
            --hangdump \
            --hangdump-timeout 1m \
            --report-xunit-trx \
            --report-xunit-trx-filename e2e-test-results.trx \
            --coverage \
            --coverage-output-format cobertura \
            --coverage-output e2e-coverage.cobertura.xml
        env:
          KUBEUI_RUN_KIND_TESTS: 1

      - name: Test Avalonia E2E
        run: |
          dotnet test \
            --project tests/KubeUI.Avalonia.Tests/KubeUI.Avalonia.Tests.csproj \
            --configuration Release \
            --no-build \
            --filter-trait "Category=Kind" \
            --hangdump \
            --hangdump-timeout 1m \
            --report-xunit-trx \
            --report-xunit-trx-filename avalonia-e2e-test-results.trx \
            --coverage \
            --coverage-output-format cobertura \
            --coverage-output avalonia-e2e-coverage.cobertura.xml
        env:
          KUBEUI_RUN_KIND_TESTS: 1

      - name: Upload test dump
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: test-diagnostics
          path: TestResults/

      - name: Test Report
        uses: dorny/test-reporter@v3
        if: (!cancelled())
        with:
          name: KubeUI Tests
          path: TestResults/*.trx
          reporter: dotnet-trx

      - name: Test Summary
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: (!cancelled())
        with:
          check_name: KubeUI Tests
          comment_mode: always
          files: TestResults/*.trx

      - name: Coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: TestResults/*.cobertura.xml
          fail_ci_if_error: false
