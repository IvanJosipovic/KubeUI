# Resource Visualization

## Current Behavior
- Visualization is cluster-scoped and initialized from a selected workspace.
- Keep graph/resource-node behavior in the visualization view model rather than the view.
