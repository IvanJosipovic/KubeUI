<UserControl
    x:Class="KubeUI.Avalonia.Resources.Network.v1.NetworkPolicy.Views.PropertiesView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:assets="using:KubeUI.Avalonia.Assets"
    xmlns:c="using:KubeUI.Avalonia.Features.Resources.Properties.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:k8s.Models"
    d:DesignHeight="250"
    d:DesignWidth="450"
    x:DataType="models:V1NetworkPolicy"
    mc:Ignorable="d">
    <StackPanel>
        <c:PropertyItem Key="{x:Static assets:Resources.NetworkPolicyPropertiesView_Pod_Selector_Labels}" Value="{Binding Spec.PodSelector.MatchLabels.Count}" />
        <c:PropertyItem Key="{x:Static assets:Resources.NetworkPolicyPropertiesView_Policy_Types}" Value="{Binding Spec.PolicyTypes.Count}" />
        <c:PropertyItem Key="{x:Static assets:Resources.NetworkPolicyPropertiesView_Ingress_Rules}" Value="{Binding Spec.Ingress.Count}" />
        <c:PropertyItem Key="{x:Static assets:Resources.NetworkPolicyPropertiesView_Egress_Rules}" Value="{Binding Spec.Egress.Count}" />
    </StackPanel>
</UserControl>
