@namespace KubeUI2.Components.Types

<Table PageSize="20" TableItem="V1PersistentVolume" Items="Items">
    <Column TableItem="V1PersistentVolume" Title="Name" Field="@(x => x.Metadata.Name)" Sortable="true" Filterable="true">
        <Template>
            <a href="/PersistentVolume/@context.Metadata.Name">@context.Metadata.Name</a>
        </Template>
    </Column>
    <Column TableItem="V1PersistentVolume" Title="Requests" Field="@(x => x.Status.Phase)" Sortable="true" Filterable="true">
        <Template>
            @context.Status.Phase
        </Template>
    </Column>
    <Column TableItem="V1PersistentVolume" Title="Age" Field="@(x => x.Metadata.CreationTimestamp)" Sortable="true" Filterable="true">
        <Template>
            @string.Format("{0:N0}d", (DateTime.UtcNow - context.Metadata.CreationTimestamp).Value.TotalDays)
        </Template>
    </Column>
    <Column TableItem="V1PersistentVolume" Title="Actions" Align="Align.Center">
        <Template>
            <BSButtonGroup DropdownDirection="DropdownDirection.Left">
                <BSDropdown>
                    <BSDropdownToggle Color="Color.Secondary" Size="Size.Small">⚙️</BSDropdownToggle>
                    <BSDropdownMenu>
                        <BSDropdownItem Href="javascript:void(0);" OnClick="@(async () => await Delete(context))">Delete</BSDropdownItem>
                    </BSDropdownMenu>
                </BSDropdown>
            </BSButtonGroup>
        </Template>
    </Column>
    <Pager TableItem="V1PersistentVolume" ShowPageNumber="true" ShowTotalCount="true" />
</Table>