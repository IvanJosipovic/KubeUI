<BSButtonGroup Size="Size.Small" title="Set the UI complexity level. Toggle to see more complex configuration options.">
    <BSButton Color="@(State.GetUILevel() == KubeUI2.UILevel.Basic ? Color.Secondary : Color.Active)" Size="Size.Small" OnClick="@(() => State.SetUILevel(KubeUI2.UILevel.Basic))">Basic</BSButton>
    <BSButton Color="@(State.GetUILevel() == KubeUI2.UILevel.Advanced ? Color.Secondary : Color.Active)" Size="Size.Small" OnClick="@(() => State.SetUILevel(KubeUI2.UILevel.Advanced))">Advanced</BSButton>
    <BSButton Color="@(State.GetUILevel() == KubeUI2.UILevel.Expert ? Color.Secondary : Color.Active)" Size="Size.Small" OnClick="@(() => State.SetUILevel(KubeUI2.UILevel.Expert))">Expert</BSButton>
</BSButtonGroup>

@code {
    [Inject] protected IState State { get; set; }

    protected override void OnInitialized()
    {
    }
}